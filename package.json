{
  "name": "monosolo",
  "version": "0.0.0",
  "private": true,
  "description": "**MonoSolo** ([**Mono**lith](https://signalvnoise.com/svn3/the-majestic-monolith/) + **Solo**) emphasizes using a single repository for your entire codebase, tailored specifically for solo developers and One Person Companies (OPCs).",
  "scripts": {
    "setup": "mise run setup",
    "clean": "mise run clean",
    "dev": "mise run dev",
    "core": "mise run core",
    "web:dev": "mise run web:dev",
    "web:build": "mise run web:build"
  },
  "packageManager": "pnpm@10.14.0",
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild",
      "lightningcss"
    ]
  }
}